services:
  server:
    container_name: streamvault-server
    build:
      context: ./server
    ports:
      - "3002:3001"
    volumes:
      - streamvault_data:/app/data
    environment:
      - LOG_LEVEL=info
    restart: unless-stopped

volumes:
  streamvault_data:
